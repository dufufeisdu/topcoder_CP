#include <iostream>
using namespace std;
#define vector <int> vi

class SwappingNodes
{
  public:
    vi swapNodes(vi leaves, int numberOfLeaves)
    {
        cout << "1";
        return leaves;
    }
};

s = SwappingNodes();
vi a = {1, 2, 3};
s.swapNodes(a, 3);
